exports.handler=async(event)=>{if(event.httpMethod!=='POST')return{statusCode:405,body:JSON.stringify({error:'Method not allowed'})};let body={};try{body=JSON.parse(event.body||'{}')}catch{return{statusCode:400,body:JSON.stringify({error:'Invalid JSON'})}}const records=Array.isArray(body.records)?body.records:[];const ids=new Set();let accepted=0,duplicates=0,invalid=0;for(const r of records){if(!r||!r.id||!r.name||!r.start){invalid++;continue}if(ids.has(r.id)){duplicates++;continue}ids.add(r.id);accepted++}return{statusCode:200,headers:{'Content-Type':'application/json'},body:JSON.stringify({checked:records.length,accepted,duplicates,invalid})};};
